<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_alignParentEnd="true"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <com.github.library.bubbleview.BubbleTextView
        android:id="@+id/chat_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_below="@+id/date_set_text"
        android:padding="10dp"
        android:maxWidth="300dp"
        android:text="@string/hello_general"
        android:textColor="@android:color/white"
        android:textSize="21sp"
        android:layout_marginStart="6dp"
        app:angle="8dp"
        app:arrowCenter="true"
        app:arrowHeight="8dp"
        app:arrowLocation="right"
        app:arrowPosition="14dp"
        app:arrowWidth="8dp"
        app:bubbleColor="@android:color/holo_green_dark" />

    <TextView
        android:id="@+id/date_send_text_view"
        android:text="@string/june_8_sample_date"
        android:textColor="@android:color/white"
        android:layout_toLeftOf="@+id/chat_textview"
        android:layout_alignBottom="@+id/chat_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/date_set_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="10dp"
        android:padding="10dp"
        android:background="@color/drawer_background"
        android:text="@string/general_date" />

</RelativeLayout>